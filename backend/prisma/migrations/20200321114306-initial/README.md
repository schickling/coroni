# Migration `20200321114306-initial`

This migration has been generated by Emanuel Joebstl at 3/21/2020, 11:43:06 AM.
You can check out the [state of the schema](./schema.prisma) after the migration.

## Database Steps

```sql
CREATE TYPE "EventType" AS ENUM ('HighRiskArea', 'Location', 'Interaction', 'Sympthoms', 'DiagnosedSick', 'MarkerSick');

CREATE TABLE "public"."User" (
    "currentRisk" Decimal(65,30)   ,
    "groupRisk" Decimal(65,30)   ,
    "id" SERIAL,
    "phoneNumber" text  NOT NULL DEFAULT '',
    PRIMARY KEY ("id")
) 

CREATE TABLE "public"."Event" (
    "data" text   ,
    "eventType" "EventType" NOT NULL DEFAULT 'HighRiskArea',
    "int" integer  NOT NULL ,
    "interaction" integer   ,
    "timestamp" timestamp(3)  NOT NULL DEFAULT '1970-01-01 00:00:00',
    "user" integer  NOT NULL ,
    PRIMARY KEY ("int")
) 

CREATE UNIQUE INDEX "User.phoneNumber" ON "public"."User"("phoneNumber")

ALTER TABLE "public"."Event" ADD FOREIGN KEY ("user")REFERENCES "public"."User"("id") ON DELETE CASCADE  ON UPDATE CASCADE

ALTER TABLE "public"."Event" ADD FOREIGN KEY ("interaction")REFERENCES "public"."User"("id") ON DELETE SET NULL  ON UPDATE CASCADE
```

## Changes

```diff
diff --git schema.prisma schema.prisma
migration ..20200321114306-initial
--- datamodel.dml
+++ datamodel.dml
@@ -1,0 +1,48 @@
+// This is your Prisma schema file,
+// learn more about it in the docs: https://pris.ly/d/prisma-schema
+
+datasource db {
+  provider = "postgresql"
+  url      = env("DATABASE_URL")
+}
+
+generator client {
+  provider = "prisma-client-js"
+}
+
+model User {
+  id           Int     @id @default(autoincrement())
+  // Global unique identifier!
+  phoneNumber  String  @unique
+  // Pre-Computed values for fast access.
+  currentRisk  Float?
+  groupRisk    Float?
+  events       Event[] @relation("UserEvents")
+  interactions Event[] @relation("UserInteractions")
+}
+
+model Event {
+  int         Int       @id
+  user        User      @relation("UserEvents")
+  timestamp   DateTime
+  eventType   EventType
+  interaction User?     @relation("UserInteractions")
+  // Attachments, handled by event type implementation
+  // might change
+  data        String?
+}
+
+enum EventType {
+  // Having travelled in a high risk area before
+  HighRiskArea
+  // Moving to another part of Germany.
+  Location
+  // Social interaction with another user
+  Interaction
+  // Developed Sympthoms
+  Sympthoms
+  // Diagnosed Sick
+  DiagnosedSick
+  // Marker inserted in past timeline when covid is diagnosed, to ease model calculation.
+  MarkerSick 
+}
```


